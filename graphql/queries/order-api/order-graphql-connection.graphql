query {
  orders {
    getAll(customerIdentifier: "cus_0001") {
      edges {
        node {
          cart {
            name
          }
        }
        cursor
      }
      pageInfo {
        totalNodes
        hasNextPage
      }
    }
  }
}
