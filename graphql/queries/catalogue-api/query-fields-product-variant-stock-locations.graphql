query {
  catalogue(path: "/shop/sofas/arbour-eco", language: "en") {
    name
    ... on Product {
      variants {
        stockLocations {
          identifier
          name
          stock
        }
      }
    }
  }
}
