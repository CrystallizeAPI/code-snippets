query {
  catalogue(path: "/stories/use-your-houseplants-as-decor", language: "en") {
    name
    component(id: "intro") {
      name
      content {
        ... on RichTextContent {
          plainText
          html
          json
        }
      }
    }
  }
}
