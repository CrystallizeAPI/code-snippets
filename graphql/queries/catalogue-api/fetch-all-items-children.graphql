query {
  catalogue(path: "/", language: "en") {
    children {
      name
      type
      children {
        name
        type
        children {
          name
          type
        }
      }
    }
  }
}
