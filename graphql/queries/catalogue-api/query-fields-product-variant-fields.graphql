query {
  catalogue(path: "/shop/sofas/arbour-eco", language: "en") {
    name
    ... on Product {
      variants {
        name
        price
        stock
        attributes {
          attribute
          value
        }
      }
    }
  }
}
