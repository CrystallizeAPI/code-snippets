query {
  catalogue(path: "/shop/sofas/arbour-eco", language: "en") {
    name
    ... on Product {
      vatType {
        ... on VatInfo {
          name
          percent
        }
      }
    }
  }
}
