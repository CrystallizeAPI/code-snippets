query {
  search(
    language: "en"
    filter: { include: { paths: "/shop/plants" } }
    orderBy: { field: ITEM_NAME, direction: ASC }
  ) {
    edges {
      node {
        name
      }
    }
  }
}
