query {
  search(
    filter: {
      searchTerm: "table"
      type: PRODUCT
      include: {
        topicPaths: {
          logicalOperator: AND
          sections: [
            { fields: { value: "/room/livingroom" } }
            {
              logicalOperator: OR
              fields: [{ value: "/color/black" }, { value: "/color/brown" }]
            }
          ]
        }
      }
      productVariants: { isDefault: true }
    }
  ) {
    edges {
      node {
        id
        name
        ... on Product {
          matchingVariant {
            price
            name
          }
        }
      }
    }
  }
}
