query {
  search(language: "en", filter: { include: { paths: "/shop/plants" } }) {
    edges {
      node {
        name
      }
      cursor
    }
    pageInfo {
      hasNextPage
      endCursor
      totalNodes
    }
  }
}
